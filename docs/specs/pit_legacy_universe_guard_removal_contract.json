{
  "schema_version": "1.0",
  "feature": "pit_legacy_universe_guard_removal",
  "domain": "data",
  "target_file": "src/daily/collect.py",
  "context_files": [
    "src/daily/collect.py",
    "tests/unit/daily/test_collect.py",
    "scratch/probe_pit_rebuild_backlog_followup.json",
    "scratch/spec_proto1/prototype.diff"
  ],
  "changes": [
    {
      "name": "PANEL_LEGACY_UNIVERSE_LAST_DATE",
      "signature": "PANEL_LEGACY_UNIVERSE_LAST_DATE: pd.Timestamp = pd.Timestamp(\"2026-09-04\")",
      "kind": "deleted_constant",
      "target_file": "src/daily/collect.py"
    },
    {
      "name": "load_history_complete_codes",
      "signature": "def load_history_complete_codes(*, path: str | os.PathLike[str] | None = None, legacy_last_date: pd.Timestamp = PANEL_LEGACY_UNIVERSE_LAST_DATE) -> frozenset[str]",
      "kind": "deleted_function",
      "target_file": "src/daily/collect.py"
    },
    {
      "name": "resolve_eligible_codes",
      "signature": "async def resolve_eligible_codes(client: Any, session: Any, decision_date: pd.Timestamp) -> frozenset[str]",
      "kind": "modified_function",
      "target_file": "src/daily/collect.py"
    }
  ],
  "wiring": [
    {
      "caller_file": "src/daily/collect.py",
      "anchor": "    prev = await resolve_prev_trading_day_kis(client, session, decision_date)\n    listed = load_eligible_codes(decision_date, prev_trading_day=prev)",
      "import_symbol": "load_eligible_codes",
      "invocation_expression": "load_eligible_codes(decision_date, prev_trading_day=prev)"
    }
  ],
  "requirements": [
    "Delete the constant PANEL_LEGACY_UNIVERSE_LAST_DATE and its preceding Korean rationale comment entirely (the two blank-line-separated lines directly above `def load_eligible_codes(`).",
    "Delete the function load_history_complete_codes entirely (its full body, including docstring), which sits between load_eligible_codes and resolve_eligible_codes.",
    "Rewrite resolve_eligible_codes to: keep the same signature `async def resolve_eligible_codes(client: Any, session: Any, decision_date: pd.Timestamp) -> frozenset[str]`; shorten its docstring to the single line `\"\"\"Resolve eligibility through the async KIS calendar before quoting.\"\"\"` (drop the now-inaccurate 'excluding history-gap symbols until the PIT panel rebuild' clause); keep the `resolve_prev_trading_day_kis` call and `load_eligible_codes` call exactly as they are; remove the `complete = load_history_complete_codes()` line, the `eligible = listed & complete` intersection line, and return `listed` directly; replace the INFO log line with exactly `logger.info(\"[DATA] stage=eligibility n_listed=%d\", len(listed))`.",
    "Do not touch load_eligible_codes, filter_eligible_candidates, resolve_prev_trading_day_kis, or any other function in src/daily/collect.py.",
    "In tests/unit/daily/test_collect.py, remove the single line `monkeypatch.setattr(collect, \"load_history_complete_codes\", lambda **_kw: frozenset({\"005930\"}))` from exactly these three existing test functions, changing nothing else in them: test_main_filters_candidates_by_eligibility_before_quoting, test_resolve_eligible_codes_passes_kis_resolved_prev_day_to_panel_lookup, test_main_resolves_previous_trading_day_through_kis_before_eligibility.",
    "In tests/unit/daily/test_collect.py, delete the two test functions test_load_history_complete_codes_returns_legacy_universe_symbols and test_resolve_eligible_codes_intersects_history_complete_codes in full, and add the two new scenario functions test_resolve_eligible_codes_returns_all_listed_codes_without_history_filter and test_legacy_universe_guard_symbols_are_fully_deleted exactly as given in scenarios (placed where the deleted tests were, in any order).",
    "No other file references PANEL_LEGACY_UNIVERSE_LAST_DATE or load_history_complete_codes (verified by repo-wide grep during /probe); no other production or test file needs changes.",
    "No try/except Exception introduced anywhere in this change."
  ],
  "design_rationale": {
    "alternatives_considered": [
      "Item1: keep the guard but make legacy_last_date auto-advance to 'today' each run (rejected: defeats its own purpose - would then exclude nothing, so it becomes a no-op that is more confusing to keep than to delete)",
      "Item1: replace with a generic min-history-days eligibility check (rejected: no such check exists anywhere else in the pipeline for any other short-history symbol; introducing one now is unrequested scope beyond restoring the pre-bug behavior)"
    ],
    "chosen_reason": "Delete PANEL_LEGACY_UNIVERSE_LAST_DATE/load_history_complete_codes and simplify resolve_eligible_codes to just load_eligible_codes - the guard's protective condition (synthetic missing-history for symbols that actually had years of real trading history but were out of the old panel's scope) no longer exists after the KRX-bulk PIT rebuild, and the guard's fixed-date design turns it into a permanent future-IPO exclusion bug if left in place: legacy_last_date never advances, so every symbol listed after it would be excluded from candidate eligibility forever.",
    "failure_modes": [
      "Item1: a symbol delisted before the panel start could theoretically vanish from both load_eligible_codes and any complete-codes check simultaneously - already naturally handled since load_eligible_codes alone requires presence on prev_trading_day, no separate guard needed"
    ]
  },
  "scenarios": [
    {
      "scenario_id": "test_main_filters_candidates_by_eligibility_before_quoting",
      "target_test_file": "tests/unit/daily/test_collect.py",
      "execution_command": "uv run pytest tests/unit/daily/test_collect.py -k test_main_filters_candidates_by_eligibility_before_quoting -q",
      "expected_behavior": "Existing test (body unchanged except the removed load_history_complete_codes monkeypatch line) keeps passing.",
      "test_skeleton": "def test_main_filters_candidates_by_eligibility_before_quoting(monkeypatch) -> None:\n    import asyncio\n    from datetime import datetime\n    from unittest.mock import AsyncMock\n\n    import pandas as pd\n    import pytest\n\n    from src.daily import collect\n\n    class _Stop(Exception):  # noqa: N818 - contract skeleton name\n        pass\n\n    class _FrozenDatetime(datetime):\n        @classmethod\n        def now(cls, tz=None):\n            return datetime(2026, 9, 14, 15, 20, 5, tzinfo=tz)\n\n    class _FakeKis:\n        def __init__(self, *args, **kwargs):\n            pass\n\n        async def ensure_token(self, session):\n            return None\n\n        async def get_market_index_rate(self, session, code):\n            return {\"rt_cd\": \"1\"}\n\n    async def _trading_day(_client, _session, _date):\n        return True\n\n    scanned = [\n        {\"code\": \"005930\", \"name\": \"삼성전자\", \"price\": \"70000\", \"chgrate\": \"3.0\"},\n        {\"code\": \"500041\", \"name\": \"ETN\", \"price\": \"+35300\", \"chgrate\": \"+8.47\"},\n    ]\n    eligibility_dates = []\n    quoted = []\n\n    def _eligible(decision_date, **_kwargs):\n        eligibility_dates.append(decision_date)\n        return frozenset({\"005930\"})\n\n    async def _fetch_all(stock_list, _client, _session):\n        quoted.append([row[\"code\"] for row in stock_list])\n        raise _Stop\n\n    monkeypatch.setattr(collect, \"datetime\", _FrozenDatetime)\n    monkeypatch.setattr(collect, \"_validate_hts_id\", lambda: None)\n    monkeypatch.setattr(collect, \"KisApiClient\", _FakeKis)\n    monkeypatch.setattr(collect, \"build_kiwoom_scan_client\", lambda: None)\n    monkeypatch.setattr(collect, \"build_toss_scan_client\", lambda: None)\n    monkeypatch.setattr(collect, \"is_kis_trading_day\", _trading_day)\n    monkeypatch.setattr(collect, \"resolve_daily_candidates\", AsyncMock(return_value=scanned))\n    monkeypatch.setattr(collect, \"load_eligible_codes\", _eligible)\n    monkeypatch.setattr(collect, \"fetch_all_stock_data\", _fetch_all)\n\n    # When\n    with pytest.raises(_Stop):\n        asyncio.run(collect.main(force=False))\n\n    # Then: 결정일 기준 적격성으로 거른 뒤에만 시세를 조회한다\n    assert eligibility_dates == [pd.Timestamp(\"2026-09-14\")]\n    assert quoted == [[\"005930\"]]\n\n\n"
    },
    {
      "scenario_id": "test_resolve_eligible_codes_passes_kis_resolved_prev_day_to_panel_lookup",
      "target_test_file": "tests/unit/daily/test_collect.py",
      "execution_command": "uv run pytest tests/unit/daily/test_collect.py -k test_resolve_eligible_codes_passes_kis_resolved_prev_day_to_panel_lookup -q",
      "expected_behavior": "Existing test (body unchanged except the removed load_history_complete_codes monkeypatch line) keeps passing.",
      "test_skeleton": "def test_resolve_eligible_codes_passes_kis_resolved_prev_day_to_panel_lookup(monkeypatch) -> None:\n    import asyncio\n\n    import pandas as pd\n\n    from src.daily import collect\n\n    async def _kis(_client, _session, _date):\n        return True\n\n    calls = []\n\n    def _load(decision_date, *, prev_trading_day, path=None):\n        calls.append((decision_date, prev_trading_day))\n        return frozenset({\"005930\"})\n\n    monkeypatch.setattr(collect, \"is_kis_trading_day\", _kis)\n    monkeypatch.setattr(collect, \"load_eligible_codes\", _load)\n\n    # When\n    out = asyncio.run(collect.resolve_eligible_codes(object(), object(), pd.Timestamp(\"2026-09-14\")))\n\n    # Then\n    assert out == frozenset({\"005930\"})\n    assert calls == [(pd.Timestamp(\"2026-09-14\"), pd.Timestamp(\"2026-09-11\"))]\n\n\n"
    },
    {
      "scenario_id": "test_main_resolves_previous_trading_day_through_kis_before_eligibility",
      "target_test_file": "tests/unit/daily/test_collect.py",
      "execution_command": "uv run pytest tests/unit/daily/test_collect.py -k test_main_resolves_previous_trading_day_through_kis_before_eligibility -q",
      "expected_behavior": "Existing test (body unchanged except the removed load_history_complete_codes monkeypatch line) keeps passing.",
      "test_skeleton": "def test_main_resolves_previous_trading_day_through_kis_before_eligibility(monkeypatch) -> None:\n    import asyncio\n    from datetime import datetime\n    from unittest.mock import AsyncMock\n\n    import pandas as pd\n    import pytest\n\n    from src.daily import collect\n\n    class _StopError(Exception):\n        pass\n\n    class _FrozenDatetime(datetime):\n        @classmethod\n        def now(cls, tz=None):\n            return datetime(2026, 9, 14, 15, 20, 5, tzinfo=tz)\n\n    class _FakeKis:\n        def __init__(self, *args, **kwargs):\n            pass\n\n        async def ensure_token(self, session):\n            return None\n\n        async def get_market_index_rate(self, session, code):\n            return {\"rt_cd\": \"1\"}\n\n    oracle_dates = []\n\n    async def _trading_day(_client, _session, date):\n        oracle_dates.append(pd.Timestamp(date).normalize())\n        return True\n\n    eligibility_calls = []\n\n    def _eligible(decision_date, *, prev_trading_day, path=None):\n        eligibility_calls.append((decision_date, prev_trading_day))\n        return frozenset({\"005930\"})\n\n    async def _fetch_all(stock_list, _client, _session):\n        raise _StopError\n\n    monkeypatch.setattr(collect, \"datetime\", _FrozenDatetime)\n    monkeypatch.setattr(collect, \"_validate_hts_id\", lambda: None)\n    monkeypatch.setattr(collect, \"KisApiClient\", _FakeKis)\n    monkeypatch.setattr(collect, \"build_kiwoom_scan_client\", lambda: None)\n    monkeypatch.setattr(collect, \"build_toss_scan_client\", lambda: None)\n    monkeypatch.setattr(collect, \"is_kis_trading_day\", _trading_day)\n    monkeypatch.setattr(\n        collect,\n        \"resolve_daily_candidates\",\n        AsyncMock(return_value=[{\"code\": \"005930\", \"name\": \"삼성전자\", \"price\": \"70000\", \"chgrate\": \"3.0\"}]),\n    )\n    monkeypatch.setattr(collect, \"load_eligible_codes\", _eligible)\n    monkeypatch.setattr(collect, \"fetch_all_stock_data\", _fetch_all)\n\n    # When\n    with pytest.raises(_StopError):\n        asyncio.run(collect.main(force=False))\n\n    # Then: 당일 확인(9/14) 후 직전 거래일(9/11)을 KIS로 확정해 적격성 조회에 전달\n    assert eligibility_calls == [(pd.Timestamp(\"2026-09-14\"), pd.Timestamp(\"2026-09-11\"))]\n    assert oracle_dates == [pd.Timestamp(\"2026-09-14\"), pd.Timestamp(\"2026-09-11\")]\n"
    },
    {
      "scenario_id": "test_resolve_eligible_codes_returns_all_listed_codes_without_history_filter",
      "target_test_file": "tests/unit/daily/test_collect.py",
      "execution_command": "uv run pytest tests/unit/daily/test_collect.py -k test_resolve_eligible_codes_returns_all_listed_codes_without_history_filter -q",
      "expected_behavior": "Replaces test_resolve_eligible_codes_intersects_history_complete_codes: resolve_eligible_codes returns load_eligible_codes' full result unfiltered, including a symbol (0220W0) that the old intersection would have dropped.",
      "test_skeleton": "def test_resolve_eligible_codes_returns_all_listed_codes_without_history_filter(monkeypatch) -> None:\n    \"\"\"PIT 재구축 이후 price_history는 전종목 이력을 보유하므로, 별도 history-complete 교집합 없이\n    직전 거래일 상장 종목이 곳 적격 종목이다 (0220W0처럼 예전엔 배제되던 코드도 그대로 통과).\"\"\"\n    import asyncio\n\n    import pandas as pd\n\n    from src.daily import collect\n\n    async def _kis(_client, _session, _date):\n        return True\n\n    monkeypatch.setattr(collect, \"is_kis_trading_day\", _kis)\n    monkeypatch.setattr(\n        collect, \"load_eligible_codes\", lambda decision_date, *, prev_trading_day, path=None: frozenset({\"005930\", \"138930\", \"0220W0\"})\n    )\n\n    # When\n    out = asyncio.run(collect.resolve_eligible_codes(object(), object(), pd.Timestamp(\"2026-09-14\")))\n\n    # Then\n    assert out == frozenset({\"005930\", \"138930\", \"0220W0\"})\n"
    },
    {
      "scenario_id": "test_legacy_universe_guard_symbols_are_fully_deleted",
      "target_test_file": "tests/unit/daily/test_collect.py",
      "execution_command": "uv run pytest tests/unit/daily/test_collect.py -k test_legacy_universe_guard_symbols_are_fully_deleted -q",
      "expected_behavior": "PANEL_LEGACY_UNIVERSE_LAST_DATE and load_history_complete_codes no longer exist on the collect module; test_load_history_complete_codes_returns_legacy_universe_symbols and test_resolve_eligible_codes_intersects_history_complete_codes are removed from this file.",
      "test_skeleton": "def test_legacy_universe_guard_symbols_are_fully_deleted() -> None:\n    from src.daily import collect\n\n    # Then: 가드 상수와 함수가 완전히 사라졌다\n    assert not hasattr(collect, \"PANEL_LEGACY_UNIVERSE_LAST_DATE\")\n    assert not hasattr(collect, \"load_history_complete_codes\")\n"
    }
  ]
}

[Unit]
Description=KCA daily audit (run once at boot, visualizes missing steps)
After=network.target

[Service]
Type=oneshot
WorkingDirectory=/home/kth/k-closing-alpha
ExecStart=/home/kth/.local/bin/uv run python -m src.tools.daily_audit

[Install]
WantedBy=default.target
